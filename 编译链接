# 编译阶段（并行）
g++ -c c.cpp -o c.o    # 1. 编译 C，-c只编译，-o输出文件，.o是目标文件（中间产物，用于链接，有未定义符号，只要所有有声明就能生成），可执行文件（所有符号已解析（无未定义符号），地址是绝对地址（加载到内存的具体位置），有入口地址（_start 或 main），可以直接运行）
g++ -c b.cpp -o b.o    # 2. 编译 B（只需要 c.h），这个阶段特别重要，搞清楚，我们只需要看到c.h就行了，因为我们在编译阶段只需要看到声明，
                                                  我们通过c.h看到了有C这个类，C的类有哪些函数，这些在编译阶段只是占位符而已，在链接的时候才确定具体的位置

# 打包阶段
ar rcs libC.a c.o      # 3. 把 c.o 打包成 libC.a

# 链接阶段
g++ main.o -o my_app libB.a libC.a # 4. 链接 B 和 C（现在需要 c.o/libC.a），这时候进行连接了就需要知道C类的函数的具体位置，这些内容就在c.o里面，生成的可执行文件
只会抽取所需要的部分，不会抽取所有的部分

链接器执行步骤：
扫描 main.o → 发现需要 B::process() 和 C::doSomething()
扫描 libB.a → 查找索引表，找到 B::process() 在 b.o 中
提取 b.o ✅
忽略 libB.a 中其他未被引用的 .o ❌
扫描 libC.a → 查找索引表，找到 C::doSomething() 在 c.o 中
提取 c.o ✅
忽略 libC.a 中其他未被引用的 .o ❌
链接：把 main.o + b.o + c.o 合并成可执行文件


编译 b.cpp 时：
┌─────────────┐
│   b.cpp     │
│ #include    │ → 展开 c.h，得到类的声明
│   "c.h"     │
│             │
│ C c;        │ → 检查：C 有声明吗？✅ 有
│ c.func();   │ → 检查：func() 有声明吗？✅ 有
│             │
│ 生成 b.o    │ → 留下未解析的符号
└─────────────┘

完全不管 c.cpp 在哪里，里面写了什么 ❌
编译 c.cpp 是另一个独立的过程：
编译 c.cpp 时：
┌─────────────┐
│   c.cpp     │
│ #include    │ → 展开 c.h
│   "c.h"     │
│             │
│ void C::    │ → 生成 C::func() 的机器码
│ func() {}   │
│             │
│ 生成 c.o    │ → 包含 C::func() 的实现
└─────────────┘



编译的时候是只要看到头文件就行了，不要看到具体的定义，报错是undeclared。而链接的时候要看到具体的实现，否则是undefined，所以你制作一个库时如果使用了另一个库，只需要有他的头文件（传递方式获取），然后就可以，这样设计主要是为了提升代码复用的功能
