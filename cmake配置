添加变量 -DCMAKE_BUILD_TYPE=.......
CMAKE_SOURCE_DIR 为最外层cmakelists.txt文件路径，CMAKE_CURRENT_SOURCE_DIR为当前CmakeList.txt的路径
CMKAE_BINARY_DIR为执行cmake ..的路径

find_package(lib Options) 查找已经通过 CMake 配置的完整库包，会自动设置库文件，头文件，如find_package(Boost REQUIRED) 找到后会自动设置Boost_INCLUDE_DIRS和Boost_LIB变量
find_library(Var libxx.a/libxx.so PATHS    ) #如果要去除在默认路径查找可以在后面使用NO_DEFULT_PATH变量，只会查找库而已

add_dependencies（targetname,libraries）这里只是指定构建顺序先要构建libraries，才能去构建targetname
target_link_libraries(${targetname} ${libs})才是真正去链接库
target_link_libraries(my_app PRIVATE A)，PRIVATE是.cpp中包含了上一级的.h文件，适合不暴露内部变量的方式，PUBLIC是包含.h文件，需要暴露外部变量的方式

#如果找到那么可以这样打印变量对不对，会打印出路径的
if(Var)
      message()
elseif()
      message()
endif()

# cmake文件中路径的问题,这样在就可以在代码中用目录级别进行包含
  是否可以通过配置
include_directories()

# cmake包含库路径
link_directories()


# 这条命令可以在执行make指令时看到具体的详细命令过程
cmake .. -DCMAKE_VERBOSE_MAKEFILE=ON


set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${CMAKE_CURRENT_SOURCE_DIR}/cmake)  设置cmake模块的查找路径，在原有的情况下进行添加路径

CMAKE_RUNTIME_OUTPUT_DIRECTORY是可执行文件输出路径（默认为空），CMAKE_ARCHIVE_OUTPUT_DIRECTORY静态库输出路径，CMAKE_LIBRARY_OUTPUT_DIRECTORY动态库输出路径

SOURCE对应的是源目录，BINARY对应的是构建目录
CMAKE_SOURCE_DIR	源代码根目录（包含最顶层 CMakeLists.txt 的目录）	/home/user/my_project
CMAKE_BINARY_DIR	构建目录根目录（运行 cmake 的目录）	/home/user/my_project/build
CMAKE_CURRENT_SOURCE_DIR	当前处理的 CMakeLists.txt 所在目录	/home/user/my_project/src
CMAKE_CURRENT_BINARY_DIR	当前 CMakeLists.txt 对应的构建目录	/home/user/my_project/build/src


target_link_libraries(... /home/.../libA.so)	✅ 能找到绝对路径	链接器接收的是文件路径，直接写入二进制
target_link_libraries(... -L/home -lA)	❌ 找不到（显示 not found）	链接器只记库名，不记路径
target_link_libraries(... yaml-cpp::yaml-cpp) + 设置 RPATH	✅ 能找到 RPATH 路径	显示为 RPATH 指定的路径
find_package + 默认情况	❌ 找不到	依赖系统默认路径，通常 /usr/lib 有才找到




